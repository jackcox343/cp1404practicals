
BoxLayout:
    orientation: 'vertical'
    Button:
        text: 'Clear'
        on_press: app.handle_clear() # Handles clearing of both input and output
    TextInput:
        id: input_name
        text: ""

    Button:
        text: 'Greet'
        on_press: app.handle_greet() # Handle greeting
    Label:
        id: output_label
        text: "Enter your name"
        color: (1,1,0,1)
